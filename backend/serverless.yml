# Welcome to Serverless!
#
# This file is the main config file for your service.
# It's very minimal at this point and uses default values.
# You can always add more config options for more control.
# We've included some commented out config examples here.
# Just uncomment any of them to get that config option.
#
# For full config options, check the docs:
#    docs.serverless.com
#
# Happy Coding!

service: feed
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

# You can pin your service to only deploy with a specific Serverless version
# Check out our docs for more details
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  memorySize: 2048
  stage: dev
  region: us-east-1

# you can overwrite defaults here
#  stage: dev
#  region: us-east-1

# you can add statements to the Lambda function's IAM Role here
#  iam:
#    role:
#      statements:
#        - Effect: "Allow"
#          Action:
#            - "s3:ListBucket"
#          Resource: { "Fn::Join" : ["", ["arn:aws:s3:::", { "Ref" : "ServerlessDeploymentBucket" } ] ]  }
#        - Effect: "Allow"
#          Action:
#            - "s3:PutObject"
#          Resource:
#            Fn::Join:
#              - ""
#              - - "arn:aws:s3:::"
#                - "Ref" : "ServerlessDeploymentBucket"
#                - "/*"

# you can define service wide environment variables here
#  environment:
#    variable1: value1

# you can add packaging information here
#package:
#  patterns:
#    - '!exclude-me.js'
#    - '!exclude-me-dir/**'
#    - include-me.js
#    - include-me-dir/**

functions:
  restApi:
    handler: handler.restApi
    events:
      - httpApi:
          path: /{proxy+}
          method: ANY

  websocket:
    handler: handler.websocket
    events:
      - websocket: $connect

    #    Define function environment variables here
    environment:
      DATABASE: mongodb+srv://Hippys:<password>@cluster0.9ljwwez.mongodb.net/feed?retryWrites=true&w=majority
      DATABASE_USERNAME: Hippys
      DATABASE_PASSWORD: AnimeTitties69

      JWT_SECRET: I-shidded-my-pants-like-twenty-times-yesterday
      JWT_EXPIRES_IN: 90d
      JWT_COOKIE_EXPIRES_IN: 90

      SESSION_SECRET: A-long-time-ago-something

      EMAIL_USERNAME: feed
      EMAIL_PASSWORD: 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
      EMAIL_HOST: smtp.sendgrid.net
      EMAIL_PORT: 587
      FRONTEND_URL: https://feed-mocha-six.vercel.app/
# you can add CloudFormation resource templates here
#resources:
#  Resources:
#    NewResource:
#      Type: AWS::S3::Bucket
#      Properties:
#        BucketName: my-new-bucket
#  Outputs:
#     NewOutput:
#       Description: "Description for the output"
#       Value: "Some output value"
